# Service report

Team: Team 2 (RFP AI) - t16
Use case: standard lab chat engine, used as a baseline before deploying our own model for the RFP project (extracting requirements from English/Arabic RFP documents)
Service and model: Qwen/Qwen2.5-1.5B-Instruct-AWQ, job "serving" (instance team-serving:8000), namespace team
Measured requests or tasks: chat completion requests generated by traffic.py against the baseline engine
Indicator and unit: p95 time to first token (TTFT), milliseconds
SLO target and window: keep p95 TTFT under 100 ms (diagnostic threshold, not yet a confirmed SLO), 5-minute rate() window
Measurement start and end: 2026-09-14T19:21:43Z to 2026-09-14T19:24:47Z (UTC)
Workload: traffic.py — one caller for 60s, a 30s pause, then four callers at once for 60s, max 64 output tokens, 120 requests total
Observed result and sample count: 38.95 ms p95 TTFT (instant query sample taken immediately after the workload completed), consistent with 38.85 ms and ~39 ms observed in earlier measurements under the same workload
Evidence: instant query result captured in Grafana Explore right after traffic.py completed; the "Team service alert" rule evaluates the same query and returned state Normal (0, below the 100 ms threshold)
Conclusion: Met
Limitations: based on a short synthetic workload (traffic.py, 120 requests, max 64 output tokens), not representative of real RFP-document-length prompts. Histogram percentiles are estimates from bucket boundaries, not exact numbers. Needs retesting with realistic prompt sizes and over a longer window before this becomes a committed SLO.
Follow-up action: retest TTFT once the RFP-specific model is deployed, using realistic document-length prompts, and repeat measurement over a longer observation window.

## Measurement query

```
1000 * histogram_quantile(0.95, sum by (le) (rate(vllm:time_to_first_token_seconds_bucket{job="serving"}[5m])))
```

Evaluated as an Instant query in Grafana Explore at approximately 2026-09-14T19:25Z, immediately after traffic.py finished (END at 19:24:47Z), so the preceding 5-minute rate() window fully covers the workload. This measures only time-to-first-token as reported by the vLLM engine; it excludes total request completion time, output validity/correctness, and any HTTP-layer failures (no http_requests_total counter is available on this engine).

## Service alert

Condition and unit: p95 TTFT (ms) is above 100
Evaluation interval: 1 minute
Pending period: 2 minutes
Relationship to the SLO: directly measures the SLO indicator (p95 TTFT) against the same 100 ms diagnostic threshold documented in my-slo-targets.md; a 2-minute pending period avoids firing on a single brief spike
First response to a notification: check current engine load (active requests) and queue depth on the vLLM engine to distinguish a genuine slowdown from a short traffic burst

## Notification test

Firing received at: 2026-09-14T08:45:25Z
Resolved received at: 2026-09-14T08:50:25Z
What the test establishes: the "Lab notification test" rule's artificial signal correctly transitioned Grafana through Pending → Alerting → Normal, and both a firing and a resolved webhook message (matching alertname "Lab notification test") were received by alert-inbox, confirming the notification delivery path (Grafana → webhook → alert-inbox) works end to end. This does not establish anything about the recovery of a real model service.