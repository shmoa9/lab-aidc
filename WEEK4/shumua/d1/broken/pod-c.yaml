# Refusal C. Edit ONLY the image line (same edit as pod.yaml); diagnose the rest.
apiVersion: v1
kind: Pod
metadata:
  name: pod-c
  labels:
    app: refused
spec:
  containers:
    - name: serving
      image: <your-user>/aidc-serving:cpu-v1
      command: ["python", "-c", "import sys; sys.exit(3)"]
